# Firebase Configuration
# Place your serviceAccountKey.json in the backend directory
# Or use Google Cloud default credentials in production

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True
